// El archivo no se llama SomethingIsWrong asi que deberiamos una o renamobarar el archivo a SomethingIsWrong
// o cambiar el nombre de la clase principal a Main
public class Main {
    public static void main(String[] args) {
        // La clase rectangulo no existe entonces la instancia de la clase rectangulo no existira
        // porque no exite esa clase

        // Rectangle myRect;
        /// myRect.width = 40;
        // myRect.height = 50;
        System.out.println("myRect's area is " + 10);
    }
}
